<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Font?>

<?import javafx.scene.Cursor?>
<BorderPane xmlns="http://javafx.com/javafx"
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="org.example.Controllers.ListSkills"
            prefHeight="700.0" prefWidth="1200.0"
            style="-fx-background-color: #ffffff;">

    <!-- BARRE DE NAVIGATION SUPÉRIEURE -->
    <top>
        <VBox>
            <!-- Header -->
            <HBox style="-fx-background-color: #2c3e50; -fx-padding: 20;" spacing="20" alignment="CENTER_LEFT">
                <Label text="Gestion des Compétences"
                       style="-fx-text-fill: white; -fx-font-size: 22px; -fx-font-weight: bold;">
                    <font>
                        <Font name="System Bold" size="22.0"/>
                    </font>
                </Label>
                <Region HBox.hgrow="ALWAYS"/>
                <Label fx:id="countLabel"
                       text="0 compétence(s)"
                       style="-fx-text-fill: white; -fx-font-size: 15px; -fx-font-weight: bold;"/>
            </HBox>

            <!-- Barre d'outils -->
            <HBox style="-fx-background-color: #34495e; -fx-padding: 15;" spacing="15" alignment="CENTER_LEFT">
                <Button text="➕ Nouvelle Compétence"
                        onAction="#handleGoToAdd"
                        prefWidth="180"
                        prefHeight="40"
                        style="-fx-background-color: #27ae60; -fx-text-fill: white; -fx-font-weight: bold; -fx-font-size: 14px; -fx-background-radius: 3;">
                    <cursor>
                        <Cursor fx:constant="HAND"/>
                    </cursor>
                </Button>

                <Separator orientation="VERTICAL"/>

                <TextField fx:id="searchField"
                           promptText="🔍 Rechercher par nom ou description..."
                           prefWidth="350"
                           style="-fx-pref-height: 35; -fx-font-size: 13px;"/>

                <ComboBox fx:id="filterCategorie"
                          promptText="Filtrer par catégorie"
                          prefWidth="180"
                          style="-fx-pref-height: 35; -fx-font-size: 13px;"/>

                <Region HBox.hgrow="ALWAYS"/>

                <Button text="🔄 Rafraîchir"
                        onAction="#handleRafraichir"
                        prefWidth="130"
                        prefHeight="35"
                        style="-fx-background-color: #3498db; -fx-text-fill: white; -fx-font-weight: bold; -fx-font-size: 13px; -fx-background-radius: 3;">
                    <cursor>
                        <Cursor fx:constant="HAND"/>
                    </cursor>
                </Button>
            </HBox>
        </VBox>
    </top>

    <!-- CONTENU PRINCIPAL - TABLEAU -->
    <center>
        <VBox spacing="0">
            <padding>
                <Insets top="0" right="0" bottom="0" left="0"/>
            </padding>

            <!-- TableView -->
            <TableView fx:id="skillTable" VBox.vgrow="ALWAYS">
                <columns>
                    <TableColumn fx:id="idColumn" text="ID" prefWidth="80.0" style="-fx-alignment: CENTER;"/>
                    <TableColumn fx:id="nomColumn" text="Nom de la compétence" prefWidth="250.0"/>
                    <TableColumn fx:id="descriptionColumn" text="Description" prefWidth="450.0"/>
                    <TableColumn fx:id="categorieColumn" text="Catégorie" prefWidth="150.0" style="-fx-alignment: CENTER;"/>
                    <TableColumn fx:id="levelColumn" text="Niveau requis" prefWidth="150.0" style="-fx-alignment: CENTER;"/>
                </columns>
                <placeholder>
                    <VBox alignment="CENTER" spacing="10">
                        <Label text="📋" style="-fx-font-size: 48px;"/>
                        <Label text="Aucune compétence disponible"
                               style="-fx-text-fill: #95a5a6; -fx-font-size: 16px;"/>
                        <Label text="Cliquez sur 'Nouvelle Compétence' pour en ajouter une"
                               style="-fx-text-fill: #bdc3c7; -fx-font-size: 13px;"/>
                    </VBox>
                </placeholder>
            </TableView>
        </VBox>
    </center>

    <!-- PIED DE PAGE - ACTIONS -->
    <bottom>
        <VBox>
            <!-- Barre d'actions -->
            <HBox style="-fx-background-color: #ecf0f1; -fx-padding: 15;" spacing="15" alignment="CENTER_RIGHT">
                <Label fx:id="statusLabel"
                       style="-fx-font-size: 13px; -fx-font-weight: bold;"
                       HBox.hgrow="ALWAYS"/>

                <Button text="✏️ Modifier"
                        onAction="#handleModifier"
                        prefWidth="140"
                        prefHeight="40"
                        style="-fx-background-color: #f39c12; -fx-text-fill: white; -fx-font-weight: bold; -fx-font-size: 14px; -fx-background-radius: 3;">
                    <cursor>
                        <Cursor fx:constant="HAND"/>
                    </cursor>
                </Button>

                <Button text="🗑️ Supprimer"
                        onAction="#handleSupprimer"
                        prefWidth="140"
                        prefHeight="40"
                        style="-fx-background-color: #e74c3c; -fx-text-fill: white; -fx-font-weight: bold; -fx-font-size: 14px; -fx-background-radius: 3;">
                    <cursor>
                        <Cursor fx:constant="HAND"/>
                    </cursor>
                </Button>
            </HBox>

            <!-- Footer -->
            <HBox style="-fx-background-color: #2c3e50; -fx-padding: 10;" alignment="CENTER">
                <Label text="© 2026 Système de Gestion RH - Module Compétences"
                       style="-fx-text-fill: #95a5a6; -fx-font-size: 11px;"/>
            </HBox>
        </VBox>
    </bottom>

</BorderPane>